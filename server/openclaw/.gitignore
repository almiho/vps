# macOS
.DS_Store

# SSH private keys — stored at /home/node/.openclaw/config/ssh/ (outside git)
config/ssh/github
# SQLite databases
data/*.db
data/*.db-wal
data/*.db-shm
# Logs and media
logs/
media/
# Nested git repos — track files, not repos
agents/infrastructure/.git
agents/*/. git
