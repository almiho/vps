# Test configuration for VPS security checks
# SSH alias as defined in your ~/.ssh/config
TARGET_HOST_ALIAS="vps"

# Public IP or DNS name for external checks (used by audit.sh)
# Set this to your server's public IP or hostname.
TARGET_PUBLIC_IP="72.62.158.189"
